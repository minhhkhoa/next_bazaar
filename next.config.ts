import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["bizweb.dktcdn.net", "res.cloudinary.com", "bizweb.dktcdn.net"], //- cấu hình domain cho image
  },
};

export default nextConfig;
